# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536m
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

libGroupId=org.mozilla.telemetry
libRepositoryName=glean
libProjectName=glean
libProjectDescription=Firefox Glean Telemetry
libUrl=https://github.com/mozilla/glean
libVcsUrl=https://github.com/mozilla/glean.git

libLicense=MPL-2.0
libLicenseUrl=https://www.mozilla.org/en-US/MPL/2.0/

android.useAndroidX=true

# Opt in to Android Gradle plugin 9 defaults ahead of the upgrade, so the
# behavior changes are absorbed here rather than all at once.
#
# android.dependency.useConstraints=false is left at the AGP 8 default: with it off,
# checkClasspath fails to fingerprint its own compileVersionMap input on Gradle 9.5.
#
# android.onlyEnableUnitTestForTheTestedBuildType is also left off, so the release and
# withoutLib unit tests keep running. Adopt it with the AGP 9 upgrade.
android.default.androidx.test.runner=true
android.enableAppCompileTimeRClass=true
android.proguard.failOnMissingFiles=true
android.r8.globalOptionsInConsumerRules.disallowed=true
android.r8.optimizedResourceShrinking=true
android.r8.proguardAndroidTxt.disallowed=true
android.r8.strictFullModeForKeepRules=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=true
android.sourceset.disallowProvider=true
android.uniquePackageNames=true
